# Dependencias del sidecar Python de Pulse (versiones fijadas).
# Instaladas dentro del runtime embebido por scripts/setup-python-runtime.js.
# Licencias: vosk (Apache-2.0), sounddevice (MIT) — aptas para redistribucion.
# NOTA: el TTS (Piper) NO va por pip: piper-phonemize no tiene wheels cp312.
# Se usa el binario standalone piper.exe (scripts/install-piper-voice.js).
#
# vosk 0.3.45 no publico wheel para macOS (solo Windows/Linux); en macOS se usa
# 0.3.44, cuya wheel universal2 cubre Intel y Apple Silicon.
vosk==0.3.45; sys_platform != 'darwin'
vosk==0.3.44; sys_platform == 'darwin'
sounddevice==0.5.1
# Transcripcion de reuniones en vivo (meeting_transcription.py). Whisper via
# CTranslate2: sin PyTorch, wheels para Windows/macOS/Linux, licencia MIT.
# El modelo (~480MB "small") se descarga en el primer uso a userData/models.
faster-whisper==1.2.0
# Diarizacion de hablantes (embeddings CAM++ 3D-Speaker via onnxruntime).
# Sin PyTorch; wheels Win/macOS/Linux; licencia Apache-2.0. El modelo (~28MB)
# lo descarga Electron a userData/models/speaker.
sherpa-onnx==1.13.4
